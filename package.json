{
  "name": "aureon-terminal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free WebGPU chart terminal with public market data and an auditable numerical core",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.mjs",
    "check": "node --check src/app.js && node --check src/chart.js && node --check src/renderer.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
