{
  "name": "aureon-terminal",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Native WebGPU trading workspace, bounded scripting, deterministic simulation and private server services",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-standalone.mjs",
    "check": "node scripts/check.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
